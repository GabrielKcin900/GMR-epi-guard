# Deps minimas para west twister -p native_sim (EPI Guard M6).
# Nao instale zephyr/scripts/requirements.txt inteiro — extras traz spsdk/hidapi.

-r ../../zephyr/scripts/requirements-base.txt
-r ../../zephyr/scripts/requirements-build-test.txt

# twisterlib (hardwaremap, relatorios)
natsort
tabulate
