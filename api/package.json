{
  "name": "epi-guard-api",
  "version": "1.0.0",
  "private": true,
  "description": "API mock EPI Guard — POST /check",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
